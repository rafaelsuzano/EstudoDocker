pipeline {
    agent {
        docker { image 'rafaelsuzano/work:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh '''npx cypress install --force
                      apt-get install npm  

                '''
            }
        }
    
        stage('Executa') {
            steps {
                sh 'npx cypress run'
            }
        }
    
    }
}
