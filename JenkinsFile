pipeline {
    agent {
        docker { image 'rafaelsuzano/demopytest' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
