pipeline {
    agent {
        docker { image 'rafaelsuzano/work:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'python --version'
            }
        }
    }
}
