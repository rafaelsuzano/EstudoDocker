pipeline {
    agent {
        docker { image 'rafaelsuzano/demopytest:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'python --version'
            }
        }
    }
}
